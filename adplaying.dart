import 'package:flutter/material.dart';

class AdPlayingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ad Playing'),
      ),
      body: Center(
        child: Text('This is the ad playing page.'),
      ),
    );
  }
}
